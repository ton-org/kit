# @ton/appkit and @ton/appkit-react currently declare mismatched @tonconnect/ui
# peer ranges (>=2.5.0-alpha.1 vs >=2.4.1), which npm's strict resolver rejects.
# legacy-peer-deps lets npm install the scaffolded project.
legacy-peer-deps=true
