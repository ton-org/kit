name: Basic build and tests

on:
  push:
  # Allows manual workflow runs with branch selection in UI
  workflow_dispatch: {}


jobs:
  unit:
    name: unit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # runs-on: [self-hosted, k8s, linux]
    # container:
    #   image: node:22-slim
    #   options: --user root

    steps:

    - name: Check out code
      uses: actions/checkout@v5
    
    - name: Read .nvmrc
      run: echo "NVMRC=$(cat .nvmrc)" >> $GITHUB_ENV

    - uses: pnpm/action-setup@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '${{ env.NVMRC }}'
        cache: 'pnpm'

    - name: Set up dependencies
      run: pnpm i --frozen-lockfile

    - name: test
      run: pnpm quality
      env:
        WALLET_MNEMONIC: ${{ secrets.WALLET_MNEMONIC }}

  mcp-integration:
    name: mcp-integration
    needs: unit
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:

    - name: Check out code
      uses: actions/checkout@v5

    - name: Read .nvmrc
      run: echo "NVMRC=$(cat .nvmrc)" >> $GITHUB_ENV

    - uses: pnpm/action-setup@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '${{ env.NVMRC }}'
        cache: 'pnpm'

    - name: Set up dependencies
      run: pnpm i --frozen-lockfile

    # The tests import @ton/walletkit, whose entry points live in dist/.
    - name: Build workspace dependencies
      run: pnpm turbo build --filter=@ton/mcp^...

    # The suite skips itself when TON_MCP_INTEGRATION_MNEMONIC is not set (e.g. on forks).
    - name: MCP integration tests (testnet, emulated sends)
      run: pnpm --filter @ton/mcp test:integration
      env:
        TON_MCP_INTEGRATION_MNEMONIC: ${{ secrets.TON_MCP_INTEGRATION_MNEMONIC }}
        TONCENTER_API_KEY: ${{ secrets.TONCENTER_API_KEY }}

  e2e-web:
    needs: unit
    uses: ./.github/workflows/e2e_web.yml
    secrets: inherit

  e2e-extension:
    needs: unit
    uses: ./.github/workflows/e2e_extension.yml
    secrets: inherit
